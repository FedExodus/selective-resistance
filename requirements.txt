# Exact pins for the study. `requirements.lock.txt` is the full `pip freeze` of the validated env.
tinker==0.27.1
tinker-cookbook==0.5.7
datasets==5.0.1
transformers==5.5.4
huggingface-hub
numpy==2.4.6
PyYAML==6.0.3
pytest==9.1.1
